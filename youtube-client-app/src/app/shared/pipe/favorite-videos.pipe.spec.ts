import { FavoriteVideosPipe } from './favorite-videos.pipe';

describe('FavoriteVideosPipe', () => {
  it('create an instance', () => {
    const pipe = new FavoriteVideosPipe();
    expect(pipe).toBeTruthy();
  });
});
